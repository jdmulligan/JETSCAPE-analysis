# Common parameters
debug_level: 0

# Scan over all combinations of specified parameter values
parameter_scan:

  # pthat bins are required, and should come first
  pt_hat_bins:
    label: 'pt_hat_bins'
    spacing: '      '
    values: [5, 7, 10, 20, 30, 40, 60, 80, 100, 120, 140, 170, 200, 250, 300, 350, 400, 500, 600, 800, 1000]

  # additional parameters are optional
  parameter1:
    label: 'eCM'
    spacing: '      '
    values: [2760, 5020]

# Generation
xml_user_file: '/home/jetscape-user/JETSCAPE/config/jetscape_user_PP19.xml'
xml_master_file: '/home/jetscape-user/JETSCAPE/config/jetscape_master.xml'

# Analysis
jetR: [0.2, 0.4]
min_jet_pt: 20.
abs_jet_eta_max: 1.
n_event_max: 10000

reader: hepmc

scale_histograms: True
merge_histograms: True
